@page
@using API.Controllers
@using APPCORE.Security
@using DataBaseModel
@{
    string? identify = HttpContext?.Session.GetString("sessionKey");
    var pageConfig = Config.pageConfig();
    if (!SecurityController.HavePermission(identify, Permissions.REPORTE_ACCESS))
    {
        Response.Redirect("/");
        return;
    }
}
<script type="module">
    import { Historial_PagosNoAutorizadosReportView } from "/Gestion_Pagos/Historial_PagosNoAutorizadosReportView.mjs";
    window.onload = () => {
        Main.append(new Historial_PagosNoAutorizadosReportView());
    } 
</script>
<div id="Main"></div>
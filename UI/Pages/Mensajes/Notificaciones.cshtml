@page
@using API.Controllers
@using APPCORE.Security
@using DataBaseModel
@{
    string? identify = HttpContext?.Session.GetString("sessionKey");
    var pageConfig = Config.pageConfig();
    if (!SecurityController.HavePermission(identify, Permissions.NOTIFICACIONES_READER))
    {
        Response.Redirect("./");
        return;
    }
}
<script type="module">
    import {NotificacionesView} from "/Gestion_Mensajes/NotificacionesView.js";
    window.onload = ()=> {
        MainN.append(new NotificacionesView());
    } 
</script>
<div id="MainN"></div>